# BCR Lineage Tracer — Python dependencies
# Install with:
#   pip install -r requirements.txt
#
# Python 3.9+ required.
# tkinter is part of the Python standard library (ships with the
# official python.org installers on Windows and macOS).
# On Linux you may need to install it via your package manager — see README.

biopython
matplotlib
numpy
openpyxl
pandas
pytest